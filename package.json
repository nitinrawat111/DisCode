{
  "name": "discode",
  "version": "1.0.0",
  "packageManager": "pnpm@9.15.9",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "start": "turbo run start",
    "lint": "turbo run lint",
    "pretty": "turbo run pretty",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^2.8.20"
  }
}
